import streamlit as st
import datetime
import cv2
import numpy as np
from PIL import Image
from fpdf import FPDF
try:
    from engine import * 
except ImportError:
    st.error("Erro Crítico: Arquivo engine.py não encontrado. Certifique-se de que ele está na mesma pasta.")

# --- CLASSE DE ARQUITETURA GENESIS (v16.0) ---
class GenesisIridologia:
    def __init__(self):
        self.setup_ui()
        self.identificacao = {}

    def setup_ui(self):
        st.set_page_config(page_title="IRIDOLOGIA & IRISDIAGNOSE PRO", layout="wide", page_icon="🔬")
        st.markdown("""
            <style>
            .stApp { background: radial-gradient(circle at top right, #050a18, #000000); }
            .main-title {
                color: #A51C30; font-family: 'Inter', sans-serif; font-size: 38px; font-weight: 800;
                border-left: 5px solid #1e3a8a; padding-left: 15px; margin-bottom: 30px;
            }
            .img-container { border: 2px solid #1e3a8a; border-radius: 12px; background: #000; padding: 10px; }
            </style>
        """, unsafe_allow_html=True)

    def motor_trava_iris_ia(self, imagem_pil):
        """ Localiza a íris independente da posição com Scan Multivariado """
        try:
            img_cv = cv2.cvtColor(np.array(imagem_pil), cv2.COLOR_RGB2BGR)
            h, w = img_cv.shape[:2]
            gray = cv2.cvtColor(img_cv, cv2.COLOR_BGR2GRAY)
            detectado = None
            
            # Lista de varredura fixa e protegida para evitar SyntaxError
            for b in:
                blurred = cv2.GaussianBlur(gray, (b, b), 0)
                circles = cv2.HoughCircles(blurred, cv2.HOUGH_GRADIENT, 1.2, 100, 
                                           param1=50, param2=35, minRadius=int(h/10), maxRadius=int(h/2))
                if circles is not None:
                    detectado = np.round(circles[0, 0]).astype("int")
                    break

            if detectado is not None:
                cx, cy, cr = detectado, detectado, detectado[2]
                margem = int(cr * 3.0)
                y1, y2 = max(0, cy - margem), min(h, cy + margem)
                x1, x2 = max(0, cx - margem), min(w, cx + margem)
                crop = img_cv[y1:y2, x1:x2]
                return Image.fromarray(cv2.cvtColor(crop, cv2.COLOR_BGR2RGB))
        except Exception as e:
            st.warning(f"Ajuste Automático: {e}")
        return imagem_pil

    def executar(self):
        st.markdown("<div class='main-title'>IRIDOLOGIA E IRISDIAGNOSE</div>", unsafe_allow_html=True)

        # 1. DASHBOARD DO PACIENTE
        with st.expander("👤 DASHBOARD DO PACIENTE", expanded=True):
            c1, c2, c3, c4 = st.columns(4)
            self.identificacao['nome'] = c1.text_input("NOME COMPLETO", key="n16")
            self.identificacao['idade'] = c2.text_input("IDADE", key="i16")
            self.identificacao['peso'] = c3.text_input("PESO (KG)", key="p16")
            self.identificacao['altura'] = c4.text_input("ALTURA (M)", key="a16")

        # 2. COMMAND CENTER (SIDEBAR)
        with st.sidebar:
            st.markdown("<h2 style='color: #A51C30;'>COMMAND CENTER</h2>", unsafe_allow_html=True)
            m_iri = st.toggle("🔬 Módulo Iridologia Master", value=False)
            m_super = st.toggle("🧠 Orquestração Neural IA", value=False)
            st.divider()
            st.caption("GENESIS FORENSIC v16.0 | Harvard Edition")

        # 3. ESTAÇÃO MASTER
        if m_iri:
            st.subheader("🔬 ESTAÇÃO IRIDOLOGIA MASTER")
            col_in, col_viz = st.columns([1, 1.2], gap="large")
            
            with col_in:
                f = st.radio("MODALIDADE", ["📁 ARQUIVO/VÍDEO", "📸 CÂMERA"], horizontal=True)
                ent = st.file_uploader("Importar Mídia", type=['jpg','png','jpeg','mp4','mov']) if f == "📁 ARQUIVO/VÍDEO" else st.camera_input("Scanner")

            if ent:
                with col_viz:
                    try:
                        img_raw = Image.open(ent)
                        img_focada = self.motor_trava_iris_ia(img_raw)
                        # Funções externas do engine.py (Mantidas)
                        img_hd = extrair_qualidade_maxima(img_focada) 
                        
                        t1, t2 = st.columns(2)
                        if t1.checkbox("🔍 Zoom Analítico", value=True): img_hd = aplicar_zoom_inteligente(img_hd)
                        if t2.checkbox("🗺️ Jensen Overlay"): img_hd = aplicar_mapa_iridologico(img_hd)
                        
                        st.markdown('<div class="img-container">', unsafe_allow_html=True)
                        st.image(img_hd, caption="Detecção Sentinel v16.0", use_container_width=True)
                        st.markdown('</div>', unsafe_allow_html=True)

                        # RELATÓRIO HARVARD
                        if st.button("⚡ GENERATE HARVARD EXECUTIVE REPORT"):
                            pdf = FPDF()
                            pdf.add_page()
                            pdf.set_fill_color(165, 28, 48); pdf.rect(0, 0, 210, 35, 'F')
                            pdf.set_text_color(255, 255, 255); pdf.set_font("Arial", 'B', 20)
                            pdf.cell(0, 15, "IRISDIAGNOSE CLINICAL REPORT", ln=True, align='C')
                            
                            pdf.set_text_color(0, 0, 0); pdf.ln(25); pdf.set_font("Arial", 'B', 12)
                            pdf.cell(0, 10, f"PACIENTE: {self.identificacao['nome'].upper() or 'NÃO IDENTIFICADO'}", ln=True)
                            
                            img_hd.save("temp_report.jpg")
                            pdf.image("temp_report.jpg", x=55, y=100, w=100)
                            
                            st.session_state['pdf_v16'] = pdf.output(dest='S').encode('latin-1', 'replace')
                            st.success("Dossiê Harvard gerado com sucesso!")

                        if 'pdf_v16' in st.session_state:
                            st.download_button("📥 BAIXAR RELATÓRIO", st.session_state['pdf_v16'], f"Report_{self.identificacao['nome']}.pdf")
                    except Exception as ex:
                        st.error(f"Erro no processamento: {ex}")

# Inicialização Blindada
if __name__ == "__main__":
    app = GenesisIridologia()
    app.executar()
